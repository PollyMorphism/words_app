<div class="mx-auto md:w-2/3 w-full">
  <%= render "shared/breadcrumbs", deck: @deck, breadcrumbs: @breadcrumbs %>
  <h1 class="font-bold text-4xl">Editing deck</h1>
  
  <%= render "form", deck: @deck %>
</div>
