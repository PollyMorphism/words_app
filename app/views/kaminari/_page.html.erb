<% if page.current? %>
  <span class="text-gray px-3 rounded"><%= page %></span>
<% else %>
  <%= link_to page, url, remote: remote, rel: page.rel, class: "px-3 text-blue-600 hover:text-blue-800" %>
<% end %>
